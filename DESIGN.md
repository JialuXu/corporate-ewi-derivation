# 设计说明

本文是框架的公开设计说明，代码注释中的 `DESIGN.md §x` 均指向本文章节。
工程使用方式（安装、CLI、目录结构）见 [README](README.md)。

## 1. 目标与边界

从多源原子指标出发，自动衍生**候选预警规则指标**，并让每条衍生指标都能被翻译成业务语言。

四个限定条件决定了所有设计取舍：

| 限定 | 含义 | 被排除的方向 |
|---|---|---|
| 对公 | 企业客户，样本量小、行业差异大 | 零售大样本方法 |
| 贷后 | 已放款客户的持续监测 | 贷前准入 / 审批 |
| 预警 | 提前发现风险，结果是可触发的规则 | 定价、额度 |
| 可解释 | 每条规则必须能被风控人员读懂和复核 | 黑盒模型、仅输出分数的评分卡 |

产出的是**候选**指标，只用于辅助人工研判；上线前必须经过人工复核与独立验证。

## 2. 总体架构

```
L1 原子指标层 ─→ L2 算子库 + 类型系统 ─→ L3 搜索引擎 ─→ L4 LLM 解释 ─→ (L5 业务交互)
                                              │                  │
                                              └──── analysis/ 跨批次台账 / 元分析
                                                    knowledge/ 行业知识 RAG
```

L1–L4 在本仓库实现；L5 不在本仓库范围。

## 3. L1 原子指标层

每个原子指标带以下元数据：

| 字段 | 作用 |
|---|---|
| `dtype` | 金额 / 计数 / 天数 / 比率 / 分类 / 布尔 / 文本 / 日期，供 L2 做合法性检查 |
| `time_grain` / `update_freq` | 日 / 季 / 年 / 事件型，决定可用的时间窗口 |
| `value_range` | 取值范围，供数据校验 |
| `business_tag` | 业务归类，供 L4 解释和覆盖度分析 |
| `null_semantics` | 空值的业务含义（见 §8 陷阱 2） |

没有这层类型信息，搜索会产出"司法案件数 ÷ 联系电话"这类无意义组合。

仓库只附带一份用公开通用术语编写的示例清单 `data/registry/metrics.example.csv`；
实际使用时通过 `AD_REGISTRY_CSV` 在运行时加载使用方自己的清单。

物理存储是长表 Parquet（`entity_id, observation_date, metric_id, value`），
由 DuckDB `PIVOT` 成宽表后交给 Polars 求值。当前 `value` 只有一列 `float64`，
因此分类 / 文本 / 日期类指标暂不能进入搜索，会被明确列在 `unsupported_fields` 中而非静默丢弃。

## 4. L2 算子库与类型系统

### 4.1 算子类别

| 类别 | 算子 |
|---|---|
| 时序变化 | `Delta`, `PctChange`, `Slope` |
| 时序异常 | `Mean`, `Std`, `ZScore`, `TsRank` |
| 计数频次 | `Count`, `DaysSince` |
| 同群比较 | `PeerRank`, `PeerDeviation` |
| 阈值逻辑 | `GT`, `LT`, `Gt2`, `Lt2`, `AND`, `OR`, `NOT`, `IfThenElse` |
| **跨源比较** | `Ratio`, `Inconsistency` |
| 关联（传染） | `AffilSum`, `AffilHas`（仅注册签名，待实体关系图层） |
| 算术 | `Add`, `Subtract`, `Multiply`, `Min`, `Max` |

**跨源比较**是最关键的一类：同一经营事实往往由多个来源分别反映（如财报营收与账户入账流水），
两者背离常常比任何单一来源的恶化更早暴露风险，也是识别披露不一致的主要手段。

### 4.2 类型签名

每个算子声明输入 / 输出类型签名，每次生成或变异表达式都做类型检查，
不合法的候选在求值前即被剔除，搜索空间因此大幅缩小。

### 4.3 硬约束

- 树深 ≤ 5，叶子 ≤ 8（可配置）
- 季 / 年粒度的财务字段，时间窗口 ≥ 1 个季度
- 量纲黑名单：金额与计数不能直接相除等
- 需要区分"未授权 / 无记录"的字段，应放在显式 `IfThenElse` 保护中（当前仅告警）

## 5. L3 搜索引擎

### 5.1 按行业 × 标签分片

每次搜索只针对一个 `(标签, 行业)` 切片，**不做全局混合搜索**（见 §8 陷阱 4）。
内置标签：`Y_overdue_3m`（3 个月内逾期）、`Y_downgrade_6m`（6 个月内五级分类下迁）、
`Y_npl_12m`（12 个月内进入不良）、`Y_early_signal`（6 个月内出现司法 / 征信不利变化）。

### 5.2 多目标适应度

手写的类型化遗传规划（GP），用 NSGA-II 求 Pareto 前沿。适应度为 6 维（均为越大越好）：

| 维度 | 含义 |
|---|---|
| IV | 区分度 |
| KS_oot | 样本外（OOT）区分度 |
| stability | `1 / (1 + PSI)`，另以 KL / Wasserstein 交叉校验 |
| monotonicity | 分箱后风险单调性 |
| complexity_neg | `-叶子数`，偏好简单规则 |
| redundancy_neg | `-max |Spearman ρ(新指标, 已有指标)|` |

冗余项必不可少：没有它，搜索会反复"重新发明"已有规则。

### 5.3 LLM 教练

LLM 是教练而不是替代品，承担三个角色：

1. **种子生成**：搜索前根据业务常识提出初始表达式
2. **剪枝建议**：对新个体打分，作为 IV 的软调节（中性分 0.5 不改变适应度）
3. **跨源提案**：每隔若干代提出跨源组合，经类型检查后注入种群

教练的任何失败（网络、JSON 解析、非法表达式）都降级为中性结果，不中断搜索。

### 5.4 时间切分

训练 / 验证 / OOT 严格按时间先后切分，不随机打乱；OOT 必须晚于所有训练数据。

## 6. L4 解释

### 6.1 解释卡片

LLM 接收表达式树、字段元数据和实测统计量，输出业务卡片：
中文指标名（≤ 15 字）、业务解释、适用场景、与已有指标的差异、阈值建议、复核要点，
并显式声明引用了哪些字段（`referenced_fields`）。

### 6.2 机器一致性校验

LLM 的解释不能直接采信（见 §8 陷阱 5）。每张卡片都经过机器校验：

- **字段幻觉**：解释中提到的字段必须真实出现在表达式树中
- **阈值方向**：树中是 `GT` 而解释写成"低于阈值时触发"即判为反向
- **统计量漂移**：解释中引用的 IV / KS 与实测偏差过大即告警

此外应保留人工"解释不准"的反馈通道。

## 7. 跨批次分析与行业知识

- `analysis/`：每次搜索可写入 append-only 的 Parquet 台账（按行业分区），
  在此之上计算跨批次全局 Pareto 前沿，并由 LLM 做语义聚类、冗余识别、覆盖缺口和五类陷阱自检。
- `knowledge/`：行业知识语料 + TF-IDF 字符 n-gram 检索，为可疑度评分提供行业背景（RAG）。

## 8. 五类陷阱

实践中反复出现的五类问题，任何改动都不应重新引入：

1. **只用 PSI 衡量稳定性**：对公样本小，PSI 波动大，需用 KL / Wasserstein 交叉验证。
2. **把"征信无查询"与"征信查询失败"当成同一个空值**：两者风险含义完全不同，`null_semantics` 必须保留区分。
3. **用同期特征预测"提前 N 个月发现"的目标**：特征必须滞后于观察点，否则得到的是伪强指标。
4. **跨行业混合搜索**：制造、批零、建筑等行业的财务常态差异很大，混搜会产生不可解释的规则。
5. **不做校验就采信 LLM 解释**：必须有表达式树与解释之间的一致性检查。
